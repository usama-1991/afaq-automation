feat: application-grade mobile responsiveness for all screens

- globals.css: comprehensive media query with semantic class hooks for all pages
- settings: scrollable horizontal tab bar, niche grid 2-col, WA IDs stack
- dashboard: greeting text ellipsis clip, subtitle 2-line clamp
- contacts: split-pane stacks, profile header stacks, detail grid 1-col
- templates: split-pane hooks, detail grid 1-col, modal near-fullscreen
- agents: split-pane hooks, channels/donts/voice grids collapse to 1-col
- campaigns: header stacks, stats 2-col, table transforms to mobile cards
- reports: stats 2-col, chart layout 1-col, agent table to mobile cards
- media: tabs scroll horizontally, grid 4-to-2-col, modal full-width
- integrations: page wrapper gets bottom-nav clearance padding
